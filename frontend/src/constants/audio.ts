/** Maximum allowed audio file size in bytes (100 MB) */
export const MAX_AUDIO_FILE_SIZE = 100 * 1024 * 1024;

/** Minimum allowed audio file size in bytes (1 KB) */
export const MIN_AUDIO_FILE_SIZE = 1024;

/** Timeout in ms for HTML Audio duration detection */
export const AUDIO_DURATION_TIMEOUT_MS = 15_000;
